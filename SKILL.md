---
name: material-replacement-tablet-video
description: 将竖版素材视频完整嵌入餐饮场景图片中的实体平板屏幕，内容像素级吻合、绝不溢出四角，暗背景也能精准检测；可先用有道 TTS 生成逐条配音，再批量生成封面与验收拼图；适用于“短视频素材替换”“素材替换视频”“平板追剧视频”“视频放进平板且不能悬浮、不能有黑白边、不能溢出边框”“先生成配音再替换视频”等请求。
version: 1.1.0
---

# 短视频素材替换

把用户提供的视频当作屏幕内容，把用户提供的图片当作不可改变的实体场景。目标是让动态画面看起来真实播放在原图平板内部，而不是把矩形视频浮贴在图片表面。

## 必须满足

- 保留原图的完整平板外框、圆角、摄像头、支架、桌面接触关系和遮挡关系。
- 视频只覆盖屏幕发光区域，不覆盖边框；四角和透视必须与原屏幕一致。
- 屏幕必须铺满，不得出现黑边、白边、透明缝隙或未填满区域。
- Pad 画面与屏幕必须吻合：内容四边必须落在屏幕发光区内沿，不能盖住黑色边框，不能像贴纸浮在 Pad 外面。发现不吻合必须返工，不能交付。
- 不要另生成平板，也不要改变图片中的菜品、文字、构图或灯光。
- 源视频若有为了适配竖屏而生成的模糊上下边，先裁掉模糊区域，再嵌入。
- 每条成片使用对应的独立音频；批量任务中每个视频的配音文案和 wav 都必须不一样，不能多条共用同一条音频。可以共用同一个参考音色，但要逐条改写话术并逐条合成。时长以各自音频为准，源视频循环播放。
- 当用户只提供配音文案而未提供音频时，可使用内置有道 TTS 先生成 wav；参考音频仍为必填，不能凭空指定音色。
- 输出 H.264/AAC、1080×1920、30 fps，并保留音频内容。

## 工作流程

1. 检查所有输入文件存在，用 `ffprobe` 记录源视频尺寸、清晰内容范围及每条音频时长。
2. 逐张检查图片，不复用未经确认的坐标。记录屏幕发光区内沿四角，顺序为左上、右上、左下、右下。坐标必须落在黑色边框内侧 2-6px；若自动识别不稳，必须手动写 `--quad` 八点坐标，不得退回矩形贴图。用户给出的固定矩形坐标只能作为候选区域：先画 QC 核对，若与当前场景图真实黑屏不吻合，必须改用实测内沿或手动四角。
3. 从源视频抽取一帧检查边缘。存在模糊延展时，确定清晰区域的 `crop=w:h:x:y`；不要用加边方式适配。
4. 若用户提供的是配音文案，先按 [有道 TTS 用法](references/youdao-tts.md) 生成每条 wav；若用户只提供一个参考音色，先抽取参考音色，再为每条视频写不同话术并合成不同 wav。若用户已提供一组音频，逐条映射使用。生成音频后用 `ffprobe` 检查每个 wav 的时长和音频流，并确认没有重复复用同一个音频文件。
5. 按 [manifest 格式](references/manifest.md) 建立任务文件（精细批量）；常规单条/批量在 macOS/Linux 上直接用 `scripts/render_fit.py`（精准嵌屏 + 无光标勾重点，推荐）或 `scripts/render_fast.py`，无需 manifest。
6. **批量前先跑一遍只检测不渲染的探测**（见「精准嵌屏核心方法」），把自动检测失败/可疑的场景图挑出来用手动 `--quad` 处理。然后先渲染第一条并抽帧检查（`--preview` 自动出屏幕四角 QC 图和 4 帧拼图）。合格后继续整批；多条任务可并行跑多个渲染进程。若用户明确要求先看样片，等待确认后再继续。
7. 查看屏幕 QC 图和验收拼图，逐条核对：平板完整、四角贴合、内容只在屏幕发光区内、黑色边框仍可见、无模糊带、无黑白边、无悬浮感、图片与音频映射正确。Pad 屏幕不吻合时必须调整四角坐标重新渲染。
8. 仅当用户明确要求写入 AI 表格时，使用 `dingtalk-aitable` 技能上传视频和封面，新增或更新目标记录后立即回读验证。不得猜测表格、门店或记录。

## 精准嵌屏核心方法（实测 150/150 贴合、零溢出）

内容能否"像素级吻合、绝不溢出四角"，取决于下面三件事，务必按此执行：

1. **用「近黑 + 低纹理」检测，而不是纯灰度阈值。** 餐饮场景的桌面/橱柜常常也是暗的，纯 `gray<threshold` 会把暗背景一起框进来，导致检测失败或框错区域。屏幕的独有特征是"既近黑、又平坦（局部标准差极小）"。判据：
   ```
   core = (gray < 45) & (local_std(15x15) < 8)
   → 闭运算15 → 开运算9 → 取最大轮廓，且 0.5≤w/h≤0.95、轮廓填充率>0.80、面积>全图3%
   ```
   `scripts/render_fast.py` 与 `scripts/render_fit.py` 已内置该算法。

2. **检测框向内 inset（3–5px）再嵌内容**，保证内容严格落在屏幕内沿、黑色边框仍可见、四角绝不溢出。宁可留 1–2px 黑缝，也不能压到边框外。用户给的"固定矩形坐标"只能当候选，必须逐图实测，不能套用。

3. **批量前先跑一遍「只检测不渲染」的探测**，把失败/可疑图挑出来单独处理，不要边渲边猜：
   - 对每张场景图调用 `detect_screen`，收集 NO_DETECT 与可疑（宽高比异常、尺寸过小）的图。
   - 对这些图用 cv2 画 100px 坐标网格叠图、人工读四角，写 `--quad x0,y0,x1,y1,x2,y2,x3,y3`（左上/右上/左下/右下）补渲染；平板后仰/侧倾时四角要按透视给梯形，不要用矩形。
   - 屏幕被菜品/手/反光大面积遮挡、无法确认边界的，换图，不要硬贴。

4. **渲染后做「接触表抽检」**：每张成片抽 1 帧缩略，按卡券拼成 6×5 网格图，肉眼过一遍四角是否闭合、有无溢出/悬浮/贴错暗区，再交付。

实测：150 张暗背景场景图，该方法 149 张自动检测通过，仅 1 张（平板被汉堡挡住、可见屏幕近方形）需手动 quad。

## 勾重点动画（无鼠标光标）

用户常反馈鼠标光标不自然。改用「画圈 / 画横线 / 光圈」逐条勾出重点信息：价格用椭圆圈、每条利益点用下划线逐条画出、CTA 用脉冲光圈收尾；描边加一层深色，保证在橙/红/蓝任意海报上都显眼。`scripts/render_fit.py` 已内置，勾重点区域（价格块/利益点行/CTA）按海报饱和色带自动定位。

## 批量海报嵌屏样片法

当用户给的是卡券截图和一批餐饮平板场景图，而不是现成的屏幕内容视频时，按下面的方法做：

1. 先从卡券截图提炼公开权益，生成适合平板屏幕比例的竖版海报或长图；不要把后台长截图直接塞进平板。海报必须突出价格、折扣/原价、活动时间、地点、使用限制和 CTA。
2. 配音先写黄金 3 秒开头，口吻用大白话邻里聊天，直接制造限时囤券紧迫感。批量时每条话术换开头、语序、利益点强调或 CTA，避免重复音频。
3. 先做一条样片给用户确认画风、屏幕贴合、光圈节奏和音色。用户确认后再批量。
4. 批量时每张场景图单独检测黑屏内沿，并为每条输出 `_screen_qc.jpg` 和 `_preview.jpg`。自动检测失败的图片不要跳过交付，也不要硬贴；改为人工查看原图并写入手动四角后补渲染。
5. 屏幕内容必须动态化：海报可做轻微推拉、极小旋转、玻璃高光扫过；用「画圈 / 画横线 / 光圈」按话术依次圈选价格、时间、地点、CTA 等重点信息（默认不用鼠标光标，`render_fit.py` 已内置）。
6. 批量结束后输出 manifest 或清单，记录每条视频、QC 图、预览图和失败/返工情况；再用 `ffprobe` 抽检或全检确认 H.264/AAC、1080×1920、30fps、音频时长有效。

## 执行

生成配音（中文约 5~7 字/秒，含数字更慢；15 秒约 90~110 字。**合成一次即用，时长以音频为准，不要为凑时长反复重合成**）：

```bash
python scripts/youdao_tts.py --text "要合成的配音文案" --ref "/absolute/ref.wav" --out "/absolute/audio/voice01.wav"
```

也可以先从用户提供的人声视频或音频里抽取参考音色：

```bash
python scripts/youdao_tts.py --prepare-ref "/absolute/reference.mp4" --ref-start 0 --ref-dur 10 --set-default
```

### 快速渲染路径（macOS/Linux 默认，一条命令约 30-45 秒）

```bash
python scripts/render_fast.py --scene scene.png --page page_full.png --audio voice01.wav --out out.mp4 --preview
```

前提：`--scene` 是含黑屏实体平板的场景图；`--page` 是宽 720px 的内容长图（整页截图，如 Chrome headless `--screenshot`）。

脚本自动完成：检测平板屏幕四角并按 9:16 构图、按边缘密度规划滚动停靠点（收尾滚到底露出总结+CTA）、光标移动+点击涟漪+CTA 红框闪烁、四点透视嵌屏、轻微推镜、帧直通 ffmpeg 管道编码（`-preset fast -crf 19`，1080×1920 30fps H.264/AAC）、ffprobe 校验并输出屏幕四角 QC 图 `<out>_screen_qc.jpg` 与 4 帧验收拼图 `<out>_preview.jpg`。

可选参数：`--quad x0,y0,x1,y1,x2,y2,x3,y3`（手动指定屏幕发光区四角，顺序左上、右上、左下、右下；兼容旧矩形格式 `x0,y0,x1,y1` 但只有正面黑屏才可用）、`--stops 0,850,1680`（手动停靠点）、`--no-cursor`、`--vh 1110`（内容视口高）、`--crf/--preset/--fps`。

### 精准嵌屏 + 无光标勾重点（推荐，scripts/render_fit.py）

场景图黑屏 + 竖版海报长图 + 配音 → 1080×1920 成片；内置「近黑+低纹理」检测、inset 防溢出，去掉鼠标光标，改用画圈（价格）/画横线（利益点逐条）/光圈（CTA）勾重点，深色描边保证任意配色海报都显眼。

```bash
python scripts/render_fit.py --scene scene.png --page poster.png --audio voice.wav --out out.mp4 [--inset 5] [--preview]
```

- `--inset`：屏幕四角向内收缩像素（默认 5）。越大越不会溢出，但可能露一点黑边。
- `--quad x0,y0,x1,y1,x2,y2,x3,y3`：手动指定四角（左上/右上/左下/右下），暗背景或遮挡图自动检测失败时用。
- 勾重点区域（价格块/利益点行/CTA）由脚本按海报饱和色带自动定位，适配「限时囤券价 + 利益点 + CTA」竖版海报模板。

### 精细/批量渲染（Windows 或需逐条定制坐标时）

```powershell
pwsh -File scripts/render_tablet_batch.ps1 -Manifest C:\absolute\batch.json
```

使用 PowerShell 7 (`pwsh`) 运行以正确处理中文路径。脚本生成成片、`封面截图`、`遮罩`和 `01-N验收拼图.jpg`，并在最后用 `ffprobe` 验证数量、分辨率及音视频流。脚本不负责 AI 表格写入。

### 透视贴合与提速要点（出片慢或内容"浮"在屏上时自查）

- 屏幕若有滚转/仰角（实物平板常后仰），必须用**四点透视**：探测屏幕四边内沿（按 x 分带做亮度剖面，取边框暗带结束处），拟合成四边形，`cv2.getPerspectiveTransform` + `warpPerspective` 把内容直接 warp 进屏幕 ROI。矩形拉伸会让内容悬浮、无物理感。
- 画面生成或真实场景图都必须先验屏幕：如果 Pad 是亮屏、边框不清、屏幕被菜品/手/反光遮挡，先重新生成或更换场景图；不要在不可确认边界的 Pad 上硬贴内容。
- `--preview` 生成的 `_screen_qc.jpg` 红线必须沿屏幕内沿闭合；四角编号若落到边框外、桌面、支架或菜品上，直接判失败。
- 亮屏/反光会让黑屏自动检测失灵：改亮度剖面逐带定边；注意勿把支架球、桌面暗影算进屏幕。
- 提速三板斧：① warp/blend 只在屏幕 ROI（外接矩形）内做，掩膜和玻璃反光 overlay 预计算全程复用；② 丢弃全帧推镜（最大单帧开销），用内容内部动效（海报缓推）代替；③ 编码 `-preset veryfast -crf 18`。实测 15s/1080p 从 24-31s 降到 **8.9s**（含编码）。
- **每帧禁止 float32 混合**：`roi*(1-mask)+warp*mask` 这类 numpy 浮点写法每帧产生 6~8 个几 MB 临时数组，实测把 452 帧从 10.8s 拖到 158s。改用 uint8 原语：预先把反光层烘焙成 `REFL_ADD`（uint8），每帧只 `cv2.add(warped, REFL_ADD)` + `cv2.copyTo(warped, MASK8, out[roi])`。
- 计时要分开打印「生成」与「编码收尾」，否则分不清是 Python 慢还是 x264 慢。
- 玻璃感：预计算一条 5-10% 透明度的斜向白色高光带 warp 进屏幕，可显著增强"实体屏"质感，零边际成本。

## 停止条件

- 图片中没有可确认的实体平板屏幕。
- Pad 屏幕边界无法确认，或 `_screen_qc.jpg` 显示四角没有贴合屏幕发光区内沿。
- 源视频清晰区域无法在不加边的情况下合理铺满屏幕，且裁切会丢失核心权益信息。
- 图片、音频、数量或映射不明确并会改变交付结果。
- 需要 TTS 但没有可用参考音频，或用户不同意把参考音频与文案上传到有道服务处理。

遇到这些情况，先说明具体问题并请用户更换素材或确认映射，不要强行输出。
